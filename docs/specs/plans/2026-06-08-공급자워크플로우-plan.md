# 공급자 워크플로우 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기관 담당자가 CSV를 업로드해 데이터를 등록하면 자동으로 표준화·품질진단이 실행되고, 담당자 본인이 진단 결과를 보고 승인(개방)/반려(보류)를 직접 결정할 수 있는 "공급자 워크플로우"를 구축한다. 이와 함께 누적되는 제출·진단 데이터를 근거로 평가편람용 계획서 초안(개방계획·품질개선계획)을 자동 생성하는 기능을 추가한다.

**Architecture:** 기존 FastAPI + DuckDB + 바닐라 JS SPA 구조를 그대로 따른다. 신규 백엔드 모듈 `app/submission.py`(업로드·스키마추론·적재·상태관리), `app/planning.py`(계획서 초안 생성)를 추가하고, 기존 `app/quality.py`에 임의 테이블용 범용 진단 규칙(`RULES_GENERIC`)을, `app/database.py`에 `submissions`/`consultant_comments` 테이블 스키마를 추가한다. `app/main.py`에 8개 API를 추가하고, 프런트엔드는 새 탭 "데이터 등록·관리"를 기존 SPA 탭 패턴(`data-tab`/`class="hidden"`)과 기존 CSS 컴포넌트(`.card`, `.btn`, `.badge`, `.modal`, `.search` 등)를 그대로 재사용해 구현한다.

**Tech Stack:** FastAPI, DuckDB(`duckdb` Python 드라이버), pandas(CSV 파싱·스키마 추론), pydantic, 바닐라 JS(SPA), pytest(신규 도입 — 이 사이클부터 자동 테스트 추가)

---

## 검증 방식에 대한 메모 (시작 전 필독)

이 프로젝트에는 현재 자동화 테스트가 전혀 없다(`requirements.txt`에 `pytest` 없음, `tests/` 폴더 없음). 지금까지는 스크립트 직접 실행 + 브라우저 검증으로 동작을 확인해 왔다.

이번 사이클부터는 **백엔드 로직(스키마 추론, 상태 전이, 범용 품질 규칙, 계획서 생성)에 한해 pytest 기반 자동 테스트를 도입**한다 — 이 로직들은 입력→출력이 명확해 TDD에 적합하고, 이후 회귀를 잡는 데 비용 대비 효과가 크다. 반면 FastAPI 라우팅·SPA 화면은 기존 관행대로 서버 실행 + 브라우저(Playwright) 검증으로 확인한다 — 이 프로젝트에 API 통합 테스트 관행이 없는데 이번에 처음 두 영역(단위+API)을 동시에 도입하면 패턴이 흩어진다. 단위 테스트가 자리잡으면 다음 사이클에 API 테스트 도입을 검토한다(YAGNI).

Task 1에서 `pytest`를 `requirements.txt`에 추가하고 `tests/` 구조를 만든다.

---

## 파일 구조 개요

**신규 생성**
- `tests/__init__.py`, `tests/test_submission.py`, `tests/test_quality_generic.py`, `tests/test_planning.py`
- `app/submission.py` — CSV 파싱, 스키마 추론, DuckDB 테이블 생성, 제출 상태 관리
- `app/planning.py` — 계획서 초안 생성(개방계획·품질개선계획)

**수정**
- `requirements.txt` — `pytest` 추가
- `app/database.py` — `submissions`, `consultant_comments` 테이블 스키마를 `init_schema()`에 추가
- `app/quality.py` — `RULES_GENERIC`, `generic_rules(table_name)`, `run_quality_generic(table_name)` 추가
- `app/main.py` — 8개 API 라우트 추가, `python-multipart` 통한 파일 업로드 처리
- `web/index.html` — 새 탭 버튼 + `<section id="tab-submission">` 추가
- `web/app.js` — 새 탭 로직(모드 토글, 업로드 폼, 목록/상세, 계획서 생성 UI)

---

## Task 1: 테스트 인프라 준비

**Files:**
- Modify: `requirements.txt`
- Create: `tests/__init__.py`

- [ ] **Step 1: requirements.txt에 pytest 추가**

`requirements.txt` 끝에 한 줄 추가:

```
pytest>=8.0
```

- [ ] **Step 2: pytest 설치**

Run: `pip install -r requirements.txt`
Expected: `pytest-8.x.x`가 설치 목록에 표시됨

- [ ] **Step 3: tests 패키지 생성**

`tests/__init__.py` 파일을 빈 내용으로 생성한다.

- [ ] **Step 4: pytest가 정상 동작하는지 확인**

Run: `pytest tests/ -v`
Expected: `collected 0 items` (에러 없이 0개 수집 — 인프라가 동작한다는 뜻)

- [ ] **Step 5: 커밋**

```bash
git add requirements.txt tests/__init__.py
git commit -m "test: pytest 테스트 인프라 추가"
```

---

## Task 2: 스키마 추론 — `app/submission.py` (1단계)

업로드된 CSV의 컬럼을 스캔해 DuckDB 컬럼 타입을 추론하는 핵심 로직부터 만든다. 이 로직은 파일 I/O 없이 순수 함수로 테스트 가능하다.

**Files:**
- Create: `app/submission.py`
- Test: `tests/test_submission.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_submission.py`:

```python
import pandas as pd
from app.submission import infer_schema


def test_infer_schema_detects_basic_types():
    df = pd.DataFrame({
        "지역명": ["창원시", "진주시"],
        "연도": [2024, 2025],
        "비율": [12.5, 8.3],
    })
    schema = infer_schema(df)
    assert schema == [
        ("지역명", "VARCHAR"),
        ("연도", "BIGINT"),
        ("비율", "DOUBLE"),
    ]
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `pytest tests/test_submission.py::test_infer_schema_detects_basic_types -v`
Expected: `FAIL` — `ModuleNotFoundError: No module named 'app.submission'` 또는 `ImportError: cannot import name 'infer_schema'`

- [ ] **Step 3: 최소 구현 작성**

`app/submission.py` 새로 생성:

```python
"""
이음(EUM) 플랫폼 - 공급자 워크플로우 (L1 적재 확장)
기관 담당자가 업로드한 CSV를 파싱·스키마 추론하여 DuckDB 테이블로
적재하고, 제출(submission) 상태를 관리한다.
"""
import datetime
import uuid

import pandas as pd

from . import database as db

_DTYPE_MAP = {
    "int64": "BIGINT",
    "float64": "DOUBLE",
    "bool": "BOOLEAN",
    "object": "VARCHAR",
    "datetime64[ns]": "TIMESTAMP",
}


def infer_schema(df: pd.DataFrame) -> list[tuple[str, str]]:
    """판다스 DataFrame의 컬럼명·dtype을 DuckDB 컬럼 정의로 변환한다."""
    schema = []
    for col in df.columns:
        duck_type = _DTYPE_MAP.get(str(df[col].dtype), "VARCHAR")
        schema.append((col, duck_type))
    return schema
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `pytest tests/test_submission.py::test_infer_schema_detects_basic_types -v`
Expected: `PASS`

- [ ] **Step 5: 커밋**

```bash
git add app/submission.py tests/test_submission.py
git commit -m "feat: CSV 스키마 추론 함수 infer_schema 추가"
```

---

## Task 3: CSV 업로드 → DuckDB 테이블 생성 — `app/submission.py` (2단계)

**Files:**
- Modify: `app/submission.py`
- Test: `tests/test_submission.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_submission.py` 끝에 추가:

```python
import io

from app import database as db
from app.submission import load_csv_to_table


def test_load_csv_to_table_creates_table_and_returns_preview():
    db.init_schema()
    csv_bytes = (
        "지역명,연도,비율\n"
        "창원시,2024,12.5\n"
        "진주시,2025,8.3\n"
    ).encode("utf-8")

    result = load_csv_to_table(io.BytesIO(csv_bytes), table_name="upload_test_001")

    assert result["table_name"] == "upload_test_001"
    assert result["rows"] == 2
    assert result["schema"] == [
        ("지역명", "VARCHAR"),
        ("연도", "BIGINT"),
        ("비율", "DOUBLE"),
    ]
    assert len(result["preview"]) == 2
    assert result["preview"][0]["지역명"] == "창원시"

    # 실제로 테이블이 만들어졌는지 DuckDB에서 직접 확인
    rows = db.query("SELECT count(*) c FROM upload_test_001")
    assert rows[0]["c"] == 2

    db.execute("DROP TABLE upload_test_001")
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `pytest tests/test_submission.py::test_load_csv_to_table_creates_table_and_returns_preview -v`
Expected: `FAIL` — `ImportError: cannot import name 'load_csv_to_table'`

- [ ] **Step 3: 구현 작성**

`app/submission.py`에 추가 (파일 끝):

```python
PREVIEW_LIMIT = 20


def load_csv_to_table(file_obj, table_name: str) -> dict:
    """CSV를 읽어 스키마를 추론하고 DuckDB 테이블로 적재한 뒤 미리보기를 반환한다."""
    df = pd.read_csv(file_obj)
    schema = infer_schema(df)

    conn = db.get_conn()
    conn.execute(f"DROP TABLE IF EXISTS {table_name}")
    conn.register("_upload_df", df)
    conn.execute(f"CREATE TABLE {table_name} AS SELECT * FROM _upload_df")
    conn.unregister("_upload_df")

    preview = db.query(f"SELECT * FROM {table_name} LIMIT {PREVIEW_LIMIT}")
    return {
        "table_name": table_name,
        "rows": len(df),
        "schema": schema,
        "preview": preview,
    }


def new_table_name(tenant_id: str) -> str:
    """제출용 테이블명을 생성한다 (tenant_id + 짧은 uuid로 충돌 방지)."""
    suffix = uuid.uuid4().hex[:8]
    return f"sub_{tenant_id}_{suffix}"
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `pytest tests/test_submission.py -v`
Expected: 두 테스트 모두 `PASS`

- [ ] **Step 5: 커밋**

```bash
git add app/submission.py tests/test_submission.py
git commit -m "feat: CSV 업로드를 DuckDB 테이블로 적재하는 load_csv_to_table 추가"
```

---

## Task 4: `submissions`/`consultant_comments` 테이블 스키마 추가 — `app/database.py`

**Files:**
- Modify: `D:\업무\gd\eum_platform\app\database.py:103-109`

`init_schema()`는 `con = get_conn()`으로 받은 커넥션에 `con.execute("""CREATE TABLE IF NOT EXISTS ...""")`를 차례로 호출하는 구조다. 컬럼 타입은 전부 `VARCHAR`(TEXT 아님)·`BIGINT`/`INTEGER`/`BOOLEAN`/`DOUBLE`을 사용한다(`catalog`, `quality_results` 테이블 참조). 마지막 `usage_log` 테이블 생성 블록(database.py:103-109) 바로 다음, `def init_schema():` 함수가 끝나는 지점(110번째 줄, `con.execute("""...usage_log...""")` 다음 줄)에 동일한 패턴으로 두 테이블을 추가한다.

- [ ] **Step 1: database.py의 init_schema() 끝에 테이블 추가**

`usage_log` 테이블 생성 블록(`con.execute(""" CREATE TABLE IF NOT EXISTS usage_log (...) """)`) 바로 다음 줄에 추가:

```python
    # 공급자 워크플로우 — 제출/심사 (L1~L3 확장)
    con.execute("""
    CREATE TABLE IF NOT EXISTS submissions (
        submission_id   VARCHAR PRIMARY KEY,
        tenant_id       VARCHAR,
        title           VARCHAR,
        description     VARCHAR,
        theme           VARCHAR,
        keywords        VARCHAR,
        license         VARCHAR,
        format          VARCHAR,
        table_name      VARCHAR,
        rows            BIGINT,
        status          VARCHAR,      -- submitted / approved / rejected
        quality_summary VARCHAR,
        decision_note   VARCHAR,
        submitted_at    VARCHAR,
        decided_at      VARCHAR
    );
    """)
    con.execute("""
    CREATE TABLE IF NOT EXISTS consultant_comments (
        comment_id      VARCHAR PRIMARY KEY,
        submission_id   VARCHAR,
        comment         VARCHAR,
        created_at      VARCHAR
    );
    """)
```

- [ ] **Step 2: 스키마가 정상 생성되는지 확인하는 테스트 작성**

`tests/test_submission.py` 끝에 추가:

```python
def test_submissions_tables_exist_after_init_schema():
    db.init_schema()
    cols = db.query("PRAGMA table_info('submissions')")
    col_names = {c["name"] for c in cols}
    assert col_names == {
        "submission_id", "tenant_id", "title", "description", "theme",
        "keywords", "license", "format", "table_name", "rows", "status",
        "quality_summary", "decision_note", "submitted_at", "decided_at",
    }

    cols2 = db.query("PRAGMA table_info('consultant_comments')")
    col_names2 = {c["name"] for c in cols2}
    assert col_names2 == {"comment_id", "submission_id", "comment", "created_at"}
```

- [ ] **Step 3: 테스트 실행 → 통과 확인**

Run: `pytest tests/test_submission.py::test_submissions_tables_exist_after_init_schema -v`
Expected: `PASS` (이미 `init_schema()`를 수정했으므로 바로 통과해야 한다 — 만약 실패하면 Step 1의 SQL 컬럼명·테이블명 오타를 점검한다)

- [ ] **Step 4: 커밋**

```bash
git add app/database.py tests/test_submission.py
git commit -m "feat: submissions/consultant_comments 테이블 스키마 추가"
```

---

## Task 5: 제출 상태 관리 함수 — `app/submission.py` (3단계)

제출 생성(`submitted`), 결정 기록(`approved`/`rejected`), 코멘트 추가를 담당하는 함수들을 추가한다.

**Files:**
- Modify: `app/submission.py`
- Test: `tests/test_submission.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_submission.py` 끝에 추가:

```python
from app.submission import create_submission, record_decision, add_comment, get_submission


def _sample_meta():
    return {
        "tenant_id": "48121",
        "title": "창원시 청년 인구 현황",
        "description": "2024년 기준 창원시 행정동별 청년 인구",
        "theme": "인구",
        "keywords": "청년,인구,창원",
        "license": "CC-BY",
        "format": "CSV",
    }


def test_create_submission_inserts_row_with_submitted_status():
    db.init_schema()
    table = "sub_48121_aaaaaaaa"
    db.execute(f"DROP TABLE IF EXISTS {table}")
    db.execute(f"CREATE TABLE {table} (지역명 VARCHAR, 인구수 BIGINT)")
    db.execute(f"INSERT INTO {table} VALUES ('창원시', 100)")

    sub_id = create_submission(_sample_meta(), table_name=table, rows=1,
                               quality_summary="규칙 4종 / 오류 0건 / 통과")

    row = db.query("SELECT * FROM submissions WHERE submission_id = ?", [sub_id])[0]
    assert row["status"] == "submitted"
    assert row["tenant_id"] == "48121"
    assert row["table_name"] == table
    assert row["rows"] == 1
    assert row["submitted_at"] is not None
    assert row["decided_at"] is None

    db.execute(f"DROP TABLE {table}")


def test_record_decision_updates_status_and_decided_at():
    db.init_schema()
    table = "sub_48121_bbbbbbbb"
    db.execute(f"DROP TABLE IF EXISTS {table}")
    db.execute(f"CREATE TABLE {table} (x BIGINT)")
    sub_id = create_submission(_sample_meta(), table_name=table, rows=0,
                               quality_summary="규칙 4종 / 오류 0건 / 통과")

    record_decision(sub_id, status="approved", decision_note="이상 없음, 개방 승인")

    row = db.query("SELECT * FROM submissions WHERE submission_id = ?", [sub_id])[0]
    assert row["status"] == "approved"
    assert row["decision_note"] == "이상 없음, 개방 승인"
    assert row["decided_at"] is not None

    db.execute(f"DROP TABLE {table}")


def test_add_comment_and_get_submission_includes_comments():
    db.init_schema()
    table = "sub_48121_cccccccc"
    db.execute(f"DROP TABLE IF EXISTS {table}")
    db.execute(f"CREATE TABLE {table} (x BIGINT)")
    sub_id = create_submission(_sample_meta(), table_name=table, rows=0,
                               quality_summary="규칙 4종 / 오류 0건 / 통과")

    add_comment(sub_id, "메타데이터 키워드를 좀 더 구체화하면 좋겠습니다.")

    detail = get_submission(sub_id)
    assert detail["meta"]["submission_id"] == sub_id
    assert len(detail["comments"]) == 1
    assert "키워드" in detail["comments"][0]["comment"]

    db.execute(f"DROP TABLE {table}")
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `pytest tests/test_submission.py -k "create_submission or record_decision or add_comment" -v`
Expected: `FAIL` — `ImportError: cannot import name 'create_submission'`

- [ ] **Step 3: 구현 작성**

`app/submission.py`에 추가 (파일 끝):

```python
def _now() -> str:
    return datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")


def create_submission(meta: dict, table_name: str, rows: int, quality_summary: str) -> str:
    """메타정보 + 적재 결과 + 진단 요약으로 제출 레코드를 생성한다 (status=submitted)."""
    submission_id = uuid.uuid4().hex
    db.execute(
        "INSERT INTO submissions VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)",
        [
            submission_id, meta["tenant_id"], meta["title"], meta["description"],
            meta["theme"], meta["keywords"], meta["license"], meta["format"],
            table_name, rows, "submitted", quality_summary, None,
            _now(), None,
        ],
    )
    return submission_id


def record_decision(submission_id: str, status: str, decision_note: str = "") -> None:
    """담당자의 승인/반려 결정을 기록한다 (status: approved | rejected)."""
    db.execute(
        "UPDATE submissions SET status = ?, decision_note = ?, decided_at = ? "
        "WHERE submission_id = ?",
        [status, decision_note, _now(), submission_id],
    )


def add_comment(submission_id: str, comment: str) -> str:
    """센터(컨설팅)의 코멘트를 추가한다."""
    comment_id = uuid.uuid4().hex
    db.execute(
        "INSERT INTO consultant_comments VALUES (?,?,?,?)",
        [comment_id, submission_id, comment, _now()],
    )
    return comment_id


def get_submission(submission_id: str) -> dict:
    """제출 상세(메타 + 미리보기 + 코멘트 이력)를 반환한다."""
    meta = db.query("SELECT * FROM submissions WHERE submission_id = ?", [submission_id])[0]
    preview = db.query(f"SELECT * FROM {meta['table_name']} LIMIT {PREVIEW_LIMIT}")
    comments = db.query(
        "SELECT * FROM consultant_comments WHERE submission_id = ? ORDER BY created_at",
        [submission_id],
    )
    return {"meta": meta, "preview": preview, "comments": comments}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `pytest tests/test_submission.py -v`
Expected: 전체 `PASS`

- [ ] **Step 5: 커밋**

```bash
git add app/submission.py tests/test_submission.py
git commit -m "feat: 제출 생성·승인/반려 결정·코멘트·상세조회 함수 추가"
```

---

## Task 6: 범용 품질 진단 규칙 — `app/quality.py` 확장

기존 `RULES`(데모 3개 테이블 전용 고정 규칙)와 동일한 인터페이스로, 임의 테이블의 컬럼을 스캔해 규칙을 동적으로 만드는 `generic_rules(table_name)`과 이를 실행하는 `run_quality_generic(table_name)`을 추가한다.

**Files:**
- Modify: `D:\업무\gd\eum_platform\app\quality.py:35` (기존 `RULES = {...}` 딕셔너리 정의가 34번째 줄 `}`로 끝난다 — 그 바로 다음 빈 줄에 새 코드를 삽입한다. `import datetime`과 `from . import database as db`는 이미 파일 상단에 있으므로 추가 import 불필요)
- Test: `tests/test_quality_generic.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_quality_generic.py` 새로 생성:

```python
from app import database as db
from app.quality import generic_rules, run_quality_generic


def _make_test_table():
    db.init_schema()
    table = "qg_test_table"
    db.execute(f"DROP TABLE IF EXISTS {table}")
    db.execute(f"""
        CREATE TABLE {table} (
            지역명 VARCHAR,
            인구수 BIGINT,
            비율   DOUBLE
        )
    """)
    db.execute(f"INSERT INTO {table} VALUES ('창원시', 100, 12.5)")
    db.execute(f"INSERT INTO {table} VALUES ('진주시', NULL, 8.3)")
    db.execute(f"INSERT INTO {table} VALUES ('진주시', NULL, 8.3)")  # 중복행
    db.execute(f"INSERT INTO {table} VALUES ('통영시', -5, 1.0)")    # 음수 이상치
    return table


def test_generic_rules_returns_rule_sql_threshold_dicts():
    table = _make_test_table()
    rules = generic_rules(table)

    assert len(rules) > 0
    for r in rules:
        assert set(r.keys()) == {"rule", "sql", "threshold"}
        assert table in r["sql"]

    db.execute(f"DROP TABLE {table}")


def test_run_quality_generic_reports_detected_issues():
    table = _make_test_table()
    result = run_quality_generic(table)

    assert result["table"] == table
    assert result["checked"] > 0
    assert result["errors"] > 0          # NULL·중복·음수가 섞여 있으므로 오류가 잡혀야 함
    assert 0 <= result["error_rate"] <= 100
    assert isinstance(result["passed"], bool)
    assert len(result["detail"]) == result["rule_count"]

    db.execute(f"DROP TABLE {table}")
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `pytest tests/test_quality_generic.py -v`
Expected: `FAIL` — `ImportError: cannot import name 'generic_rules'`

- [ ] **Step 3: 구현 작성**

`app/quality.py`의 `RULES = {...}` 딕셔너리 정의 바로 다음에 추가 (기존 `RULES`를 건드리지 않고 그 아래에 새 코드를 삽입한다):

```python
RULES_GENERIC_THRESHOLD = 5.0  # 임의 업로드 데이터는 데모 3종보다 기준을 완화한다 (5%)

NUMERIC_TYPES = {"BIGINT", "INTEGER", "DOUBLE", "FLOAT", "DECIMAL", "HUGEINT"}


def generic_rules(table_name: str) -> list[dict]:
    """테이블 컬럼을 스캔해 결측치·중복·타입일관성·음수이상치 규칙을 동적으로 생성한다."""
    cols = db.query(f"PRAGMA table_info('{table_name}')")
    rules = []

    for c in cols:
        name, ctype = c["name"], c["type"]
        rules.append({
            "rule": f"결측치 비율 - {name}",
            "sql": f'SELECT count(*) FROM {table_name} WHERE "{name}" IS NULL',
            "threshold": RULES_GENERIC_THRESHOLD,
        })
        if ctype in NUMERIC_TYPES:
            rules.append({
                "rule": f"음수 이상치 - {name}",
                "sql": f'SELECT count(*) FROM {table_name} WHERE "{name}" < 0',
                "threshold": RULES_GENERIC_THRESHOLD,
            })

    col_list = ", ".join(f'"{c["name"]}"' for c in cols)
    rules.append({
        "rule": "중복행 비율",
        "sql": (
            f"SELECT count(*) - count(DISTINCT ({col_list})) "
            f"FROM {table_name}"
        ),
        "threshold": RULES_GENERIC_THRESHOLD,
    })
    return rules


def run_quality_generic(table_name: str) -> dict:
    """generic_rules()로 만든 규칙을 실행해 run_quality()와 동일한 형태의 결과를 만든다."""
    total_rows = db.query(f"SELECT count(*) c FROM {table_name}")[0]["c"]
    rules = generic_rules(table_name)

    detail = []
    errors = 0
    for r in rules:
        viol = list(db.query(r["sql"])[0].values())[0]
        errors += viol
        detail.append({"rule": r["rule"], "violations": viol, "threshold": r["threshold"]})

    checked = total_rows * len(rules) if rules else 0
    rate = (errors / checked * 100) if checked else 0.0
    passed = rate <= RULES_GENERIC_THRESHOLD

    return {
        "table": table_name,
        "rule_count": len(rules),
        "checked": checked,
        "errors": errors,
        "error_rate": round(rate, 4),
        "threshold": RULES_GENERIC_THRESHOLD,
        "passed": passed,
        "detail": detail,
        "ran_at": datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
    }
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `pytest tests/test_quality_generic.py -v`
Expected: 전체 `PASS`

- [ ] **Step 5: 커밋**

```bash
git add app/quality.py tests/test_quality_generic.py
git commit -m "feat: 임의 업로드 테이블용 범용 품질 진단 규칙 RULES_GENERIC 추가"
```

---

## Task 7: 품질 요약 문자열 생성 헬퍼 — `app/submission.py` (4단계)

`create_submission()`에 넘길 `quality_summary` 텍스트를 `run_quality_generic()` 결과로부터 만드는 헬퍼를 추가한다. (테이블에 표시·계획서 초안에 재사용하기 위한 짧은 요약 문자열)

**Files:**
- Modify: `app/submission.py`
- Test: `tests/test_submission.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_submission.py` 끝에 추가:

```python
from app.submission import summarize_quality


def test_summarize_quality_formats_pass_and_fail():
    passed = {"rule_count": 4, "checked": 400, "errors": 0, "error_rate": 0.0, "passed": True}
    failed = {"rule_count": 4, "checked": 400, "errors": 12, "error_rate": 3.0, "passed": False}

    s_pass = summarize_quality(passed)
    s_fail = summarize_quality(failed)

    assert "통과" in s_pass
    assert "4종" in s_pass and "0건" in s_pass
    assert "미통과" in s_fail
    assert "12건" in s_fail and "3.0%" in s_fail
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `pytest tests/test_submission.py::test_summarize_quality_formats_pass_and_fail -v`
Expected: `FAIL` — `ImportError: cannot import name 'summarize_quality'`

- [ ] **Step 3: 구현 작성**

`app/submission.py`에 추가 (파일 끝):

```python
def summarize_quality(result: dict) -> str:
    """품질진단 결과를 제출 레코드에 저장할 짧은 요약 문자열로 변환한다."""
    verdict = "통과" if result["passed"] else "미통과"
    return (
        f"규칙 {result['rule_count']}종 / 오류 {result['errors']}건 "
        f"/ 오류율 {result['error_rate']}% / {verdict}"
    )
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `pytest tests/test_submission.py::test_summarize_quality_formats_pass_and_fail -v`
Expected: `PASS`

- [ ] **Step 5: 커밋**

```bash
git add app/submission.py tests/test_submission.py
git commit -m "feat: 품질진단 결과 요약 문자열 생성 함수 summarize_quality 추가"
```

---

## Task 8: 계획서 초안 생성 — `app/planning.py`

`submissions` 데이터를 근거로 "공공데이터 개방계획"과 "품질개선계획" 초안 텍스트를 규칙기반으로 생성한다.

**Files:**
- Create: `app/planning.py`
- Test: `tests/test_planning.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_planning.py` 새로 생성:

```python
from app import database as db
from app.submission import create_submission, record_decision
from app.planning import draft_open_plan, draft_quality_plan


TENANT = "48121"


def _seed_submissions():
    db.init_schema()
    db.execute("DELETE FROM submissions WHERE tenant_id = ?", [TENANT])

    for i, (status, summary) in enumerate([
        ("approved", "규칙 4종 / 오류 0건 / 오류율 0.0% / 통과"),
        ("submitted", "규칙 4종 / 오류 9건 / 오류율 4.5% / 통과"),
        ("rejected", "규칙 4종 / 오류 30건 / 오류율 7.5% / 미통과"),
    ]):
        table = f"plan_test_{TENANT}_{i}"
        db.execute(f"DROP TABLE IF EXISTS {table}")
        db.execute(f"CREATE TABLE {table} (x BIGINT)")
        meta = {
            "tenant_id": TENANT, "title": f"테스트 데이터셋 {i}",
            "description": "설명", "theme": "행정", "keywords": "테스트",
            "license": "CC-BY", "format": "CSV",
        }
        sub_id = create_submission(meta, table_name=table, rows=10, quality_summary=summary)
        if status != "submitted":
            record_decision(sub_id, status=status, decision_note="검토 완료")


def test_draft_open_plan_counts_status_by_tenant():
    _seed_submissions()
    text = draft_open_plan(TENANT)

    assert "공공데이터 개방계획" in text
    assert "개방 1건" in text or "개방: 1건" in text
    assert "보류 1건" in text or "보류: 1건" in text
    assert "검토대기 1건" in text or "검토대기: 1건" in text


def test_draft_quality_plan_lists_failed_submissions():
    _seed_submissions()
    text = draft_quality_plan(TENANT)

    assert "품질개선계획" in text
    assert "테스트 데이터셋 2" in text   # 미통과(rejected) 건이 개선과제로 언급돼야 함
    assert "7.5%" in text
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `pytest tests/test_planning.py -v`
Expected: `FAIL` — `ModuleNotFoundError: No module named 'app.planning'`

- [ ] **Step 3: 구현 작성**

`app/planning.py` 새로 생성:

```python
"""
이음(EUM) 플랫폼 - 평가대응 엔진 확장: 계획서 초안 생성
실제 제출·진단 데이터를 평가편람 양식에 맞춰 규칙기반으로
텍스트 초안을 채운다 (외부 LLM 미사용 — 공공망 환경 대응).
"""
import re

from . import database as db


def _tenant_name(tenant_id: str) -> str:
    rows = db.query("SELECT name FROM tenants WHERE tenant_id = ?", [tenant_id])
    return rows[0]["name"] if rows else tenant_id


def draft_open_plan(tenant_id: str) -> str:
    """제출 현황(개방/보류/검토대기 건수)을 근거로 개방계획 초안을 만든다."""
    name = _tenant_name(tenant_id)
    rows = db.query(
        "SELECT status, title, decision_note FROM submissions WHERE tenant_id = ? "
        "ORDER BY submitted_at",
        [tenant_id],
    )

    approved = [r for r in rows if r["status"] == "approved"]
    rejected = [r for r in rows if r["status"] == "rejected"]
    pending = [r for r in rows if r["status"] == "submitted"]

    lines = [
        f"# {name} 공공데이터 개방계획 초안",
        "",
        f"- 등록 {len(rows)}건 / 개방 {len(approved)}건 / 보류 {len(rejected)}건 "
        f"/ 검토대기 {len(pending)}건",
        "",
        "## 1. 개방 완료",
    ]
    lines += [f"- {r['title']}" for r in approved] or ["- (해당 없음)"]

    lines += ["", "## 2. 보류 사유 및 향후 조치"]
    if rejected:
        lines += [f"- {r['title']}: {r['decision_note'] or '사유 미기재'}" for r in rejected]
    else:
        lines += ["- (해당 없음)"]

    lines += ["", "## 3. 향후 개방 예정(검토대기)"]
    lines += [f"- {r['title']}" for r in pending] or ["- (해당 없음)"]

    return "\n".join(lines)


_RATE_RE = re.compile(r"오류율\s*([\d.]+)%")
_RULE_RE = re.compile(r"규칙\s*(\d+)종")
_ERR_RE = re.compile(r"오류\s*(\d+)건")


def draft_quality_plan(tenant_id: str) -> str:
    """미통과(반려) 제출의 진단 요약을 분석해 품질개선계획 초안을 만든다."""
    name = _tenant_name(tenant_id)
    rows = db.query(
        "SELECT title, status, quality_summary FROM submissions "
        "WHERE tenant_id = ? AND status = 'rejected' ORDER BY submitted_at",
        [tenant_id],
    )

    lines = [
        f"# {name} 품질개선계획 초안",
        "",
        f"- 개선 검토 대상(보류) {len(rows)}건",
        "",
        "## 개선과제",
    ]
    if not rows:
        lines.append("- (현재 보류 상태인 제출이 없어 개선과제가 없습니다)")
        return "\n".join(lines)

    for r in rows:
        summary = r["quality_summary"] or ""
        rate_m = _RATE_RE.search(summary)
        err_m = _ERR_RE.search(summary)
        rate = rate_m.group(1) + "%" if rate_m else "확인 필요"
        errors = err_m.group(1) + "건" if err_m else "확인 필요"
        lines.append(
            f"- **{r['title']}** — 오류 {errors}, 오류율 {rate}: "
            f"데이터 입력 단계의 결측치·중복·이상치 점검 후 재정제하여 재진단 권장"
        )

    return "\n".join(lines)
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `pytest tests/test_planning.py -v`
Expected: 전체 `PASS`

- [ ] **Step 5: 커밋**

```bash
git add app/planning.py tests/test_planning.py
git commit -m "feat: 개방계획·품질개선계획 초안 자동 생성 모듈 추가"
```

---

## Task 9: API 라우트 추가 — `app/main.py`

8개 엔드포인트를 추가한다. 파일 업로드를 받으려면 `python-multipart`가 필요하므로 먼저 확인한다.

**Files:**
- Modify: `requirements.txt`
- Modify: `D:\업무\gd\eum_platform\app\main.py`

- [ ] **Step 1: requirements.txt에 python-multipart 추가**

FastAPI의 `UploadFile`은 내부적으로 `python-multipart`가 필요하다. `requirements.txt`에 추가:

```
python-multipart>=0.0.9
```

Run: `pip install -r requirements.txt`

- [ ] **Step 2: import 구문 확장**

`app/main.py` 상단의 import 블록을 수정한다.

기존:
```python
from fastapi import FastAPI, Query
```
변경 후:
```python
from fastapi import FastAPI, Query, UploadFile, File, Form
```

기존:
```python
from . import database as db
from . import seed_data, quality, evaluation, ontology, nlquery
```
변경 후:
```python
from . import database as db
from . import seed_data, quality, evaluation, ontology, nlquery, submission, planning
```

- [ ] **Step 3: 8개 라우트 추가**

`# ---------- 정적 프론트엔드 ----------` 주석 바로 위(기존 `nl()` 함수 다음)에 아래 블록을 통째로 삽입한다:

```python
# ---------- 공급자 워크플로우 ----------
@app.post("/api/submission/upload")
def submission_upload(file: UploadFile = File(...), tenant_id: str = Form(...)):
    table_name = submission.new_table_name(tenant_id)
    result = submission.load_csv_to_table(file.file, table_name=table_name)
    return result


@app.post("/api/submission")
def submission_create(
    tenant_id: str = Form(...),
    title: str = Form(...),
    description: str = Form(...),
    theme: str = Form(...),
    keywords: str = Form(...),
    license: str = Form(...),
    format: str = Form(...),
    table_name: str = Form(...),
    rows: int = Form(...),
):
    meta = {
        "tenant_id": tenant_id, "title": title, "description": description,
        "theme": theme, "keywords": keywords, "license": license, "format": format,
    }
    diag = quality.run_quality_generic(table_name)
    summary = submission.summarize_quality(diag)
    sub_id = submission.create_submission(meta, table_name=table_name, rows=rows,
                                           quality_summary=summary)
    return {"submission_id": sub_id, "status": "submitted",
            "quality": diag, "quality_summary": summary}


@app.get("/api/submission")
def submission_list(tenant_id: str = Query(...)):
    return db.query(
        "SELECT * FROM submissions WHERE tenant_id = ? ORDER BY submitted_at DESC",
        [tenant_id])


@app.get("/api/submission/all")
def submission_list_all():
    return db.query("SELECT * FROM submissions ORDER BY submitted_at DESC")


@app.post("/api/submission/{submission_id}/decision")
def submission_decision(submission_id: str, status: str = Form(...),
                         decision_note: str = Form(default="")):
    submission.record_decision(submission_id, status=status, decision_note=decision_note)
    return {"submission_id": submission_id, "status": status}


@app.post("/api/submission/{submission_id}/comment")
def submission_comment(submission_id: str, comment: str = Form(...)):
    comment_id = submission.add_comment(submission_id, comment)
    return {"comment_id": comment_id, "submission_id": submission_id}


@app.get("/api/submission/{submission_id}")
def submission_detail(submission_id: str):
    detail = submission.get_submission(submission_id)
    diag = quality.run_quality_generic(detail["meta"]["table_name"])
    detail["quality"] = diag
    return detail


@app.get("/api/plan/draft")
def plan_draft(tenant_id: str = Query(...), type: str = Query(...)):
    if type == "open":
        return {"type": "open", "draft": planning.draft_open_plan(tenant_id)}
    if type == "quality":
        return {"type": "quality", "draft": planning.draft_quality_plan(tenant_id)}
    return JSONResponse({"error": "type must be 'open' or 'quality'"}, status_code=400)
```

- [ ] **Step 4: 서버를 띄워 라우트가 정상 등록되는지 확인**

Run: `cd "D:\업무\gd\eum_platform"; python run.py`

브라우저에서 `http://127.0.0.1:5959/docs` 접속.
Expected: Swagger UI에 `/api/submission/upload`, `/api/submission`, `/api/submission/{submission_id}/decision`, `/api/plan/draft` 등 8개 라우트가 모두 표시됨. 서버 콘솔에 에러 스택트레이스가 없어야 한다.

서버를 종료한다 (Ctrl+C).

- [ ] **Step 5: 커밋**

```bash
git add requirements.txt app/main.py
git commit -m "feat: 공급자 워크플로우 API 8종 라우트 추가"
```

---

## Task 10: 화면 — 새 탭 "데이터 등록·관리" 골격 추가 — `web/index.html`

탭 버튼과 섹션 골격만 먼저 추가해 SPA 탭 전환이 정상 동작하는지 확인한다.

**Files:**
- Modify: `D:\업무\gd\eum_platform\web\index.html:18` (탭 버튼), `:106-107` (섹션)

- [ ] **Step 1: 탭 버튼 추가**

`<nav id="nav">` 안의 마지막 버튼(index.html:18 `<button data-tab="ai">자연어 질의</button>`) 바로 다음 줄에 추가:

```html
<button data-tab="submission">데이터 등록·관리</button>
```

- [ ] **Step 2: 섹션 골격 추가**

마지막 섹션인 `<section id="tab-ai" class="hidden">...</section>`의 닫는 태그(index.html:106 `</section>`) 바로 다음, `</main>`(index.html:107) 이전에 추가:

```html
<section id="tab-submission" class="hidden">
  <div class="page-title">데이터 등록·관리</div>
  <div class="page-sub">기관 담당자가 데이터를 등록하고 진단·승인을 직접 결정합니다. 센터는 현황을 모니터링하고 컨설팅 의견을 남깁니다.</div>

  <div class="chips" id="sub-mode-toggle">
    <button data-mode="agency" class="active">기관 담당자 모드</button>
    <button data-mode="center">센터(컨설팅) 모드</button>
  </div>

  <div id="sub-agency-view"></div>
  <div id="sub-center-view" class="hidden"></div>
</section>
```

- [ ] **Step 3: 브라우저에서 탭 전환 확인**

Run: `cd "D:\업무\gd\eum_platform"; python run.py`

브라우저에서 `http://127.0.0.1:5959` 접속 → 새로 추가된 "데이터 등록·관리" 탭 클릭.
Expected: 탭이 활성화되고 "데이터 등록·관리" 제목과 모드 토글 버튼(기관 담당자 모드 / 센터(컨설팅) 모드)이 보인다. 콘솔에 JS 에러 없음(이 시점에는 `app.js`에 로직이 없으므로 토글 버튼 클릭은 아직 동작하지 않아도 된다).

서버를 종료한다.

- [ ] **Step 4: 커밋**

```bash
git add web/index.html
git commit -m "feat: '데이터 등록·관리' 탭 골격 추가"
```

---

## Task 11: 화면 — 모드 토글 + 기관 담당자: 새 데이터 등록 폼 — `web/app.js`

**Files:**
- Modify: `D:\업무\gd\eum_platform\web\app.js`

`app.js`는 기존 탭들의 렌더 함수(`load조회()`/`render조회()` 형태 — 정확한 함수명은 파일을 열어 기존 패턴을 확인한 뒤 그 명명 규칙을 따른다)와 탭 전환 핸들러가 있는 구조다. 아래 코드는 독립적인 IIFE 모듈로 추가해 기존 코드와 충돌을 피한다.

- [ ] **Step 1: 파일 끝에 제출 탭 모듈 추가**

`web/app.js` 파일의 맨 끝에 추가:

```javascript
// ---------- 데이터 등록·관리 탭 ----------
(function () {
  const TENANT_ID = "48121"; // 데모: 창원시 고정 (로그인 미구현 단계)
  let currentMode = "agency";

  // 파일 상단에 이미 정의된 전역 $ (querySelector)를 그대로 쓰고, $all만 추가한다
  function $all(sel, root) { return Array.from((root || document).querySelectorAll(sel)); }

  function initSubmissionTab() {
    const toggle = $("#sub-mode-toggle");
    if (!toggle || toggle.dataset.bound) return;
    toggle.dataset.bound = "1";

    $all("button", toggle).forEach((btn) => {
      btn.addEventListener("click", () => {
        $all("button", toggle).forEach((b) => b.classList.remove("active"));
        btn.classList.add("active");
        currentMode = btn.dataset.mode;
        $("#sub-agency-view").classList.toggle("hidden", currentMode !== "agency");
        $("#sub-center-view").classList.toggle("hidden", currentMode !== "center");
        if (currentMode === "agency") renderAgencyView();
        else renderCenterView();
      });
    });

    renderAgencyView();
  }

  function renderAgencyView() {
    const root = $("#sub-agency-view");
    root.innerHTML = `
      <div class="cards c2">
        <div class="card">
          <h3>새 데이터 등록</h3>
          <form id="upload-form">
            <p><input type="file" id="upload-file" accept=".csv" required></p>
            <p><button type="submit" class="btn btn-p">업로드 &amp; 미리보기</button></p>
          </form>
          <div id="upload-result"></div>
          <form id="meta-form" class="hidden">
            <p><input type="text" name="title" placeholder="제목" required></p>
            <p><input type="text" name="description" placeholder="설명" required></p>
            <p><input type="text" name="theme" placeholder="주제(예: 인구·행정·교통)" required></p>
            <p><input type="text" name="keywords" placeholder="키워드(쉼표로 구분)" required></p>
            <p><input type="text" name="license" placeholder="라이선스(예: CC-BY)" required></p>
            <p><input type="text" name="format" placeholder="형식(예: CSV)" required></p>
            <p><button type="submit" class="btn btn-p">등록(자동 진단 실행)</button></p>
          </form>
          <div id="register-result"></div>
        </div>
        <div class="card">
          <h3>내 제출 현황</h3>
          <div id="my-submissions"></div>
        </div>
      </div>
      <div class="card" id="plan-draft-card">
        <h3>계획서 초안 생성</h3>
        <div class="chips">
          <button data-plan="open" class="btn btn-o">공공데이터 개방계획 초안</button>
          <button data-plan="quality" class="btn btn-o">품질개선계획 초안</button>
        </div>
        <pre id="plan-draft-output" class="sql"></pre>
      </div>
    `;

    let uploadedTable = null;
    let uploadedRows = 0;

    $("#upload-form").addEventListener("submit", async (e) => {
      e.preventDefault();
      const fileInput = $("#upload-file");
      if (!fileInput.files.length) return;
      const fd = new FormData();
      fd.append("file", fileInput.files[0]);
      fd.append("tenant_id", TENANT_ID);
      const res = await fetch("/api/submission/upload", { method: "POST", body: fd });
      const data = await res.json();
      uploadedTable = data.table_name;
      uploadedRows = data.rows;
      $("#upload-result").innerHTML =
        `<p class="note">스키마 추론 완료: ${data.rows}행, 컬럼 ${data.schema.length}개. 아래 메타정보를 입력해 등록하세요.</p>`;
      $("#meta-form").classList.remove("hidden");
    });

    $("#meta-form").addEventListener("submit", async (e) => {
      e.preventDefault();
      if (!uploadedTable) return;
      const fd = new FormData(e.target);
      fd.append("tenant_id", TENANT_ID);
      fd.append("table_name", uploadedTable);
      fd.append("rows", String(uploadedRows));
      const res = await fetch("/api/submission", { method: "POST", body: fd });
      const data = await res.json();
      $("#register-result").innerHTML =
        `<p class="note">등록 완료 — 자동 진단 결과: ${data.quality_summary}</p>`;
      e.target.reset();
      e.target.classList.add("hidden");
      uploadedTable = null;
      loadMySubmissions();
    });

    $all("[data-plan]").forEach((btn) => {
      btn.addEventListener("click", async () => {
        const res = await fetch(`/api/plan/draft?tenant_id=${TENANT_ID}&type=${btn.dataset.plan}`);
        const data = await res.json();
        $("#plan-draft-output").textContent = data.draft;
      });
    });

    loadMySubmissions();
  }

  async function loadMySubmissions() {
    const res = await fetch(`/api/submission?tenant_id=${TENANT_ID}`);
    const rows = await res.json();
    const root = $("#my-submissions");
    if (!rows.length) {
      root.innerHTML = '<p class="note">등록된 제출이 없습니다.</p>';
      return;
    }
    root.innerHTML = rows.map((r) => `
      <div class="ds" data-id="${r.submission_id}">
        <strong>${r.title}</strong>
        <span class="badge ${r.status === 'approved' ? 'b-ok' : r.status === 'rejected' ? 'b-na' : 'b-warn'}">${r.status}</span>
        <p>${r.quality_summary || ''}</p>
      </div>
    `).join("");
    $all(".ds", root).forEach((el) => {
      el.addEventListener("click", () => openSubmissionDetail(el.dataset.id));
    });
  }

  async function openSubmissionDetail(submissionId) {
    const res = await fetch(`/api/submission/${submissionId}`);
    const detail = await res.json();
    const m = detail.meta;
    const modalRoot = $("#modal-root");
    modalRoot.innerHTML = `
      <div class="modal">
        <div class="box">
          <h3>${m.title}</h3>
          <p>${m.description}</p>
          <p><span class="badge ${m.status === 'approved' ? 'b-ok' : m.status === 'rejected' ? 'b-na' : 'b-warn'}">${m.status}</span> ${m.quality_summary || ''}</p>
          ${m.status === 'submitted' ? `
            <p>
              <button class="btn btn-p" id="btn-approve">승인(개방)</button>
              <button class="btn btn-o" id="btn-reject">반려(보류)</button>
            </p>
            <p><input type="text" id="decision-note" placeholder="결정 메모(선택)"></p>
          ` : ''}
          <h4>센터 코멘트</h4>
          ${detail.comments.length
            ? detail.comments.map((c) => `<p>- ${c.comment} (${c.created_at})</p>`).join("")
            : '<p class="note">코멘트가 없습니다.</p>'}
          <p><button class="btn btn-o" id="btn-close">닫기</button></p>
        </div>
      </div>
    `;
    $("#btn-close").addEventListener("click", () => { modalRoot.innerHTML = ""; });
    const approveBtn = $("#btn-approve");
    const rejectBtn = $("#btn-reject");
    if (approveBtn) approveBtn.addEventListener("click", () => decide(submissionId, "approved"));
    if (rejectBtn) rejectBtn.addEventListener("click", () => decide(submissionId, "rejected"));
  }

  async function decide(submissionId, status) {
    const note = $("#decision-note") ? $("#decision-note").value : "";
    const fd = new FormData();
    fd.append("status", status);
    fd.append("decision_note", note);
    await fetch(`/api/submission/${submissionId}/decision`, { method: "POST", body: fd });
    $("#modal-root").innerHTML = "";
    loadMySubmissions();
  }

  function renderCenterView() {
    const root = $("#sub-center-view");
    root.innerHTML = `<p class="note">센터(컨설팅) 모드는 다음 단계에서 구현됩니다.</p>`;
  }

  // 기존 탭 전환 핸들러(app.js:8-17)가 호출할 수 있도록 전역에 노출한다
  window.initSubmissionTab = initSubmissionTab;
})();
```

- [ ] **Step 2: 기존 탭 전환 핸들러에 새 탭 초기화 연결**

`web/app.js`의 기존 탭 전환 핸들러(파일 상단, "/* ---------- 탭 전환 ---------- */" 블록)는 탭 클릭 시 `if (b.dataset.tab === 'ontology') loadOntology();` `if (b.dataset.tab === 'portal') searchCatalog();` 형태로 탭별 초기화 함수를 호출한다. 같은 패턴으로 한 줄을 추가한다.

기존 (app.js:14-15):
```javascript
    if (b.dataset.tab === 'ontology') loadOntology();
    if (b.dataset.tab === 'portal') searchCatalog();
```
변경 후:
```javascript
    if (b.dataset.tab === 'ontology') loadOntology();
    if (b.dataset.tab === 'portal') searchCatalog();
    if (b.dataset.tab === 'submission') window.initSubmissionTab();
```

- [ ] **Step 3: 브라우저에서 업로드 → 미리보기 → 등록 → 진단 → 목록 표시 흐름 확인**

Run: `cd "D:\업무\gd\eum_platform"; python run.py`

1. `http://127.0.0.1:5959` → "데이터 등록·관리" 탭 클릭
2. 임시 CSV 파일(예: `지역명,인구수\n창원시,100\n진주시,80`)을 업로드
3. "업로드 & 미리보기" 클릭 → "스키마 추론 완료: 2행..." 노트 표시 확인
4. 메타정보 입력 후 "등록(자동 진단 실행)" 클릭 → "등록 완료 — 자동 진단 결과: 규칙 N종 / 오류 M건 ..." 표시 확인
5. "내 제출 현황"에 새 항목이 `submitted` 배지와 함께 나타나는지 확인
6. 항목 클릭 → 모달에서 "승인(개방)" 클릭 → 모달이 닫히고 목록의 배지가 `approved`로 바뀌는지 확인

Expected: 위 6단계가 에러 없이 모두 동작. 브라우저 콘솔에 JS 에러 없음.

서버를 종료한다.

- [ ] **Step 4: 커밋**

```bash
git add web/app.js
git commit -m "feat: 기관 담당자 모드 — 업로드·등록·진단·승인/반려·계획서초안 UI 추가"
```

---

## Task 12: 화면 — 센터(컨설팅) 모드: 전체 현황 + 코멘트 — `web/app.js`

**Files:**
- Modify: `D:\업무\gd\eum_platform\web\app.js`

- [ ] **Step 1: `renderCenterView` 교체**

Task 11에서 추가한 자리표시자 함수를 찾아 교체한다.

기존:
```javascript
  function renderCenterView() {
    const root = $("#sub-center-view");
    root.innerHTML = `<p class="note">센터(컨설팅) 모드는 다음 단계에서 구현됩니다.</p>`;
  }
```

변경 후:
```javascript
  function renderCenterView() {
    const root = $("#sub-center-view");
    root.innerHTML = `
      <div class="card">
        <h3>전체 제출 현황</h3>
        <div id="center-summary"></div>
      </div>
      <div class="card">
        <h3>전체 제출 목록</h3>
        <div id="center-list"></div>
      </div>
    `;
    loadCenterView();
  }

  async function loadCenterView() {
    const res = await fetch("/api/submission/all");
    const rows = await res.json();

    const total = rows.length;
    const approved = rows.filter((r) => r.status === "approved").length;
    const rejected = rows.filter((r) => r.status === "rejected").length;
    const pending = rows.filter((r) => r.status === "submitted").length;

    $("#center-summary").innerHTML = `
      <div class="cards c4">
        <div class="card stat"><div class="num">${total}</div><div class="lbl">전체 등록</div></div>
        <div class="card stat"><div class="num">${approved}</div><div class="lbl">개방</div></div>
        <div class="card stat"><div class="num">${rejected}</div><div class="lbl">보류</div></div>
        <div class="card stat"><div class="num">${pending}</div><div class="lbl">검토대기</div></div>
      </div>
    `;

    const listRoot = $("#center-list");
    if (!rows.length) {
      listRoot.innerHTML = '<p class="note">등록된 제출이 없습니다.</p>';
      return;
    }
    listRoot.innerHTML = rows.map((r) => `
      <div class="ds" data-id="${r.submission_id}">
        <strong>${r.title}</strong> (${r.tenant_id})
        <span class="badge ${r.status === 'approved' ? 'b-ok' : r.status === 'rejected' ? 'b-na' : 'b-warn'}">${r.status}</span>
        <p>${r.quality_summary || ''}</p>
      </div>
    `).join("");
    $all(".ds", listRoot).forEach((el) => {
      el.addEventListener("click", () => openCenterDetail(el.dataset.id));
    });
  }

  async function openCenterDetail(submissionId) {
    const res = await fetch(`/api/submission/${submissionId}`);
    const detail = await res.json();
    const m = detail.meta;
    const modalRoot = $("#modal-root");
    modalRoot.innerHTML = `
      <div class="modal">
        <div class="box">
          <h3>${m.title} (${m.tenant_id})</h3>
          <p>${m.description}</p>
          <p><span class="badge ${m.status === 'approved' ? 'b-ok' : m.status === 'rejected' ? 'b-na' : 'b-warn'}">${m.status}</span> ${m.quality_summary || ''}</p>
          <h4>컨설팅 코멘트 남기기</h4>
          <p><input type="text" id="comment-input" placeholder="개선 의견을 입력하세요"></p>
          <p><button class="btn btn-p" id="btn-comment-submit">코멘트 등록</button></p>
          <h4>코멘트 이력</h4>
          <div id="comment-history">
            ${detail.comments.length
              ? detail.comments.map((c) => `<p>- ${c.comment} (${c.created_at})</p>`).join("")
              : '<p class="note">코멘트가 없습니다.</p>'}
          </div>
          <p><button class="btn btn-o" id="btn-close">닫기</button></p>
        </div>
      </div>
    `;
    $("#btn-close").addEventListener("click", () => { modalRoot.innerHTML = ""; });
    $("#btn-comment-submit").addEventListener("click", async () => {
      const text = $("#comment-input").value.trim();
      if (!text) return;
      const fd = new FormData();
      fd.append("comment", text);
      await fetch(`/api/submission/${submissionId}/comment`, { method: "POST", body: fd });
      modalRoot.innerHTML = "";
      openCenterDetail(submissionId);
    });
  }
```

- [ ] **Step 2: 브라우저에서 센터 모드 확인**

Run: `cd "D:\업무\gd\eum_platform"; python run.py`

1. "데이터 등록·관리" 탭에서 "센터(컨설팅) 모드" 클릭
2. "전체 제출 현황" 카드에 전체/개방/보류/검토대기 건수가 표시되는지 확인
3. "전체 제출 목록"에서 항목 클릭 → 모달에서 코멘트 입력 후 "코멘트 등록" 클릭
4. 모달이 새로고침되며 입력한 코멘트가 "코멘트 이력"에 나타나는지 확인
5. "기관 담당자 모드"로 돌아가 해당 제출을 열어 같은 코멘트가 보이는지 확인 (양쪽 모드가 같은 데이터를 본다는 것 검증)

Expected: 위 5단계가 에러 없이 모두 동작.

서버를 종료한다.

- [ ] **Step 3: 커밋**

```bash
git add web/app.js
git commit -m "feat: 센터(컨설팅) 모드 — 전체 현황 모니터링·코멘트 작성 UI 추가"
```

---

## Task 13: 전체 회귀 확인 + 위키 업데이트

**Files:**
- (검증만, 코드 수정 없음)
- Modify: `D:\업무\agents\wiki\notes\project-eum-platform-gd.md`
- Modify: `D:\업무\agents\wiki\notes\sessions\세션-YYYY-MM-DD-*.md` (해당일 세션 노트가 있다면)

- [ ] **Step 1: 전체 pytest 실행**

Run: `cd "D:\업무\gd\eum_platform"; pytest tests/ -v`
Expected: 모든 테스트 `PASS` (Task 1~8에서 작성한 테스트 전부)

- [ ] **Step 2: 기존 5개 탭 회귀 확인**

Run: `cd "D:\업무\gd\eum_platform"; python run.py`

브라우저에서 기존 5개 탭(대시보드·개방포털·품질진단·온톨로지·자연어질의)을 각각 클릭해 정상 동작 확인 — 새 탭 추가로 인한 JS 충돌(특히 `app.js` 끝에 추가한 IIFE의 전역 이벤트 리스너)이 없는지 점검한다.

Expected: 5개 탭 모두 기존과 동일하게 동작, 콘솔 에러 없음.

서버를 종료한다.

- [ ] **Step 3: 위키 프로젝트 노트 업데이트**

`D:\업무\agents\wiki\notes\project-eum-platform-gd.md`의 "2단계 — 공급자 워크플로우 (설계 완료, 계획 작성 중)" 섹션 제목과 내용을 실제 구현 완료 상태로 갱신한다 — 예: "(구현 완료)"로 표시하고, 추가된 API 8종·신규 모듈·테스트 개수(이 계획의 Task 1~8에서 작성한 테스트 파일·테스트 함수 수를 `pytest tests/ -v` 결과와 대조해 정확한 개수 기재)를 반영한다. [[feedback-wiki-update-discipline]]의 체크 기준(노트의 기능 목록·테스트 수가 현재 코드와 일치하는가)을 따른다.

- [ ] **Step 4: 최종 커밋**

```bash
git add -A
git commit -m "test: 공급자 워크플로우 전체 회귀 확인 완료"
```

---

## 자가 점검 결과 (계획 작성자 메모)

- **스펙 커버리지**: 역할모델/워크플로우흐름(Task 5,11,12) · 데이터모델(Task 4) · 범용품질규칙(Task 6) · 계획서작성지원(Task 8) · API 8종(Task 9) · 화면구성(Task 10,11,12) · 모듈변경요약 전 항목 — 모두 태스크로 매핑됨.
- **플레이스홀더 점검**: "TODO/TBD/적절히 처리" 형태의 문구 없음 — 모든 단계에 실행 가능한 코드·정확한 명령·기대 결과를 명시함.
- **타입/시그니처 일관성**: `create_submission(meta, table_name, rows, quality_summary)` → Task 5/7/9에서 동일 시그니처로 사용. `run_quality_generic(table_name)` → Task 6에서 정의, Task 9·13에서 동일하게 호출. `get_submission(submission_id)`가 반환하는 `{meta, preview, comments}` 구조 → Task 9의 `/api/submission/{id}`에서 `quality` 키만 추가해 그대로 사용. 일치 확인됨.
- **Task 9 Step 3의 `database.py` 삽입 위치 안내**: 기존 코드의 정확한 호출 표현(`db.execute` vs `execute`)을 미리 단정하지 않고, 실행자가 파일을 열어 기존 패턴을 그대로 따르도록 명시함 — 추측에 의한 오류를 방지하기 위함.
