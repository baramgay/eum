# 공급자 워크플로우 구현 진행 로그

> 다른 PC·다른 AI가 이어서 작업할 수 있도록, 태스크별 상태·테스트 명령/출력·리뷰 결과·커밋 SHA를 기록한다.
> 작업 위치: `D:\업무\gd\eum_platform\.worktrees\공급자-워크플로우-impl` (브랜치 `feature/공급자-워크플로우`, 원격 https://github.com/baramgay/eum)
> 진행 방식: superpowers:subagent-driven-development — 태스크별 구현 서브에이전트 → 스펙 일치 리뷰 → 코드 품질 리뷰 → 완료 표시
> 계획서: `docs/specs/plans/2026-06-08-공급자워크플로우-plan.md` (전체 13개 태스크)

## ⚠️ 이월 주의사항 (다음 태스크에서 반드시 반영)
- **[Task 9 — 해결완료] SQL 식별자 인터폴레이션 검증**: Task 3 코드 품질 리뷰에서 지적된 사항. `submission_upload`에 `tenant_id`↔`tenants` 테이블 화이트리스트 가드 적용으로 1차 해결. 그러나 `submission_create`의 `table_name: str = Form(...)`이 동일 벡터로 재노출되어 있음을 Task 9 코드 품질 리뷰(1차)가 추가로 적발 — 커밋 `cbfc907`에서 `tenant_id` 검증 + `_TABLE_NAME_RE` 패턴/소유권 검증 가드를 `submission_create`에도 추가하여 ✅ APPROVED 재검토 완료. **두 라우트 모두 안전 확인됨 — 향후 `table_name`을 클라이언트로부터 받는 새 라우트를 추가할 경우 반드시 동일한 이중 가드(tenant_id 화이트리스트 + `sub_<tenant_id>_<8자리hex>` 패턴/소유권 검증) 적용할 것.**
- **[Task 11/12 담당자 참고] draft_quality_plan의 문자열 라운드트립 설계 — 비차단 메모**: Task 8 코드 품질 리뷰에서 지적됨 — `draft_quality_plan`은 `summarize_quality()`가 만든 문자열(`quality_summary`)을 정규식(`_RATE_RE`/`_ERR_RE`)으로 다시 파싱해 수치를 추출함. 구조화된 데이터(`run_quality_generic()` 결과의 `error_rate`/`errors`)를 문자열로 만들었다가 다시 정규식으로 역파싱하는 방식이라 `summarize_quality`의 문구·포맷이 바뀌면 매칭이 조용히 깨질 수 있음(미스매치 시 "확인 필요"로 폴백). 당장 차단 사유는 아니나, **화면(웹 UI)에서 계획서 초안을 표시/연동할 때 이 결합(coupling)을 인지**하고 `summarize_quality` 변경 시 `_RATE_RE`/`_ERR_RE`도 함께 점검할 것. (`_RULE_RE`는 스펙에 명시된 대로 미사용 상태로 보존됨 — 향후 정리 과제로 별도 기록 권장)

## 진행 상태 요약

| Task | 제목 | 상태 | 커밋 SHA |
|------|------|------|----------|
| 1 | 테스트 인프라 준비 | ✅ 완료 (스펙✅·품질✅) | `ae21cea245f690e07687a190c5d98a6b1ab0d685` |
| 2 | 스키마 추론 (app/submission.py 1단계) | ✅ 완료 (스펙✅·품질✅) | `5c150bc3bcd29ae9709b30e1f80ce817f070be25` |
| 3 | CSV 적재 (app/submission.py 2단계) | ✅ 완료 (스펙✅·품질✅·이월주의 1건) | `b7bb84a7bdd97b0e6ee15c8dbf26c2f191085227` |
| 4 | DB 스키마 — submissions/consultant_comments 테이블 | ✅ 완료 (스펙✅·품질✅) | `891d4ac231e296fead6a1d5c8d3d2652f2e8c6ae` |
| 5 | 제출 관리 — create/record_decision/add_comment/get | ✅ 완료 (스펙✅·품질✅) | `1f7a17b3ae765b70b369e577d7048ac2fa93349e` |
| 6 | 범용 품질 규칙 — generic_rules/run_quality_generic | ✅ 완료 (스펙✅·품질✅) | `585b9a79885cdb8e14e7a7487ea25e32bf585252` |
| 7 | 품질 결과 요약 — summarize_quality | ✅ 완료 (스펙✅·품질✅) | `e08ed60ecc4be09bb2b3f585940d17788e508c9c` |
| 8 | 계획서 초안 생성 — app/planning.py | ✅ 완료 (스펙✅·품질✅·이월주의 1건 추가) | `b1f3066d19360f5c7d9ef6e394dd27fe7e783199` |
| 9 | API 8종 — app/main.py 라우트 추가 | ✅ 완료 (스펙✅·품질✅·재검토 1회) | `06379ac`(API), `cbfc907`(보안수정) |
| 10 | 화면 — 새 탭 골격 (web/index.html) | ✅ 완료 (스펙✅·품질✅) | `163b862aada36bfd0bb2c9f7387866298b561f54` |
| 11 | 화면 — 기관 담당자 등록 폼 (web/app.js) | ✅ 완료 (스펙✅·품질✅·재검토 1회) | `7fc86b0`(UI), `bbd9c21`(XSS수정) |
| 12 | 화면 — 센터 모드 현황+코멘트 (web/app.js) | ⏳ 대기 | - |
| 13 | 전체 회귀 확인 + 위키 업데이트 | ⏳ 대기 | - |

---

## Task 1: 테스트 인프라 준비 — ✅ 완료 (2026-06-08)

**구현 내용**
- `requirements.txt` 끝에 `pytest>=8.0` 추가
- `tests/__init__.py` 빈 파일 생성

**테스트 실행 및 결과 (실측)**
```
$ pip install -r requirements.txt
Requirement already satisfied: pytest>=8.0 ... (9.0.2)

$ pytest tests/ -v
============================= test session starts =============================
platform win32 -- Python 3.11.9, pytest-9.0.2, pluggy-1.6.0
collecting ... collected 0 items
============================ no tests ran in 0.01s ============================
(exit code 5 = "no tests collected" — 정상, 인프라 정상 동작 확인)
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT
- requirements.txt에 `pytest>=8.0` 1회만 존재(중복 없음), tests/__init__.py 0바이트 확인
- 커밋이 정확히 `requirements.txt`, `tests/__init__.py` 2개 파일만 포함, 메시지 일치
- 추가 파일(pytest.ini, conftest.py 등) 없음 — 스펙 외 추가사항 없음

**코드 품질 리뷰**: ✅ APPROVED — requirements.txt 형식 일관, tests/__init__.py는 app/__init__.py와 동일 컨벤션, 커밋 메시지 conventional-commit 스타일·한자 없음. 이슈 없음

**커밋**: `ae21cea245f690e07687a190c5d98a6b1ab0d685` — "test: pytest 테스트 인프라 추가"
**진행 로그 커밋**: `bb6d7e1` — "docs: 구현 진행 로그 추가"

---

## Task 2: 스키마 추론 — app/submission.py (1단계) — ✅ 완료 (2026-06-08)

**구현 내용**
- `app/submission.py` 신규 생성: 모듈 docstring, `_DTYPE_MAP`(pandas dtype→DuckDB 타입 5종), `infer_schema(df)` 순수함수
- `tests/test_submission.py` 신규: `test_infer_schema_detects_basic_types`
- TDD 순서 준수: 테스트 먼저 작성 → 실패 확인 → 구현 → 통과 확인

**테스트 실행 및 결과 (실측)**
```
[구현 전 — 실패 확인]
ERROR collecting tests/test_submission.py
ImportError while importing test module ...
E   ModuleNotFoundError: No module named 'app.submission'
============================== 1 error in 3.97s ===============================

[구현 후 — 통과 확인]
tests/test_submission.py::test_infer_schema_detects_basic_types PASSED   [100%]
============================== 1 passed in 0.45s ==============================
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — app/submission.py·tests/test_submission.py 내용이 계획서 코드와 byte-for-byte 일치(docstring/임포트/_DTYPE_MAP/함수/타입힌트/한글 문자열 포함), 커밋이 정확히 2개 파일만 포함, TDD 실패→통과 순서 실측 확인. datetime/uuid/db 미사용 임포트는 후속 태스크(3,5,7)용으로 의도된 것 — 이슈 아님

**코드 품질 리뷰**: ✅ APPROVED — 네이밍/docstring 컨벤션이 quality.py·database.py와 일치, dict-lookup-with-default 패턴 적절, 테스트가 실제 동작(3종 dtype 분기+한글 컬럼명) 검증. Nice-to-have만 존재(타입힌트가 코드베이스 새 선례임을 인지, _DTYPE_MAP이 기본 pandas dtype만 커버 — 현재 범위에선 충분)

**커밋**: `5c150bc3bcd29ae9709b30e1f80ce817f070be25` — "feat: CSV 스키마 추론 함수 infer_schema 추가"

---

## Task 3: CSV 업로드 → DuckDB 테이블 생성 — app/submission.py (2단계) — ✅ 완료 (2026-06-08)

**구현 내용**
- `app/submission.py`에 `PREVIEW_LIMIT=20`, `load_csv_to_table(file_obj, table_name)`, `new_table_name(tenant_id)` 추가 (Task 2의 infer_schema 재사용)
- `tests/test_submission.py`에 `test_load_csv_to_table_creates_table_and_returns_preview` 추가 (실제 DuckDB 테이블 생성·조회·삭제까지 검증하는 통합 테스트)
- TDD 순서 준수

**테스트 실행 및 결과 (실측)**
```
[구현 전 — 실패 확인]
E   ImportError: cannot import name 'load_csv_to_table' from 'app.submission'
============================== 1 error in 0.58s ===============================

[구현 후 — 통과 확인 (재실행 포함 2회)]
tests/test_submission.py::test_infer_schema_detects_basic_types PASSED   [ 50%]
tests/test_submission.py::test_load_csv_to_table_creates_table_and_returns_preview PASSED [100%]
============================== 2 passed in 0.78s ===============================
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — 코드/테스트 내용 계획서와 일치(SQL 문자열·docstring·한글 문자열 포함), 커밋 정확히 2개 파일(+62), 작업트리 클린(로컬 환경설정용 data/ 디렉터리는 .gitignore로 제외 확인)

**코드 품질 리뷰**: ✅ APPROVED — register/unregister 페어링 정상, 실제 DB 테이블 생성을 직접 재조회로 검증하는 양질의 통합테스트, 네이밍 일관성 양호. SQL 식별자 인터폴레이션은 코드베이스 전체 관행과 동일(DuckDB는 식별자 파라미터화 미지원)하여 현재 시점엔 문제 없음 — **단, Task 9에서 라우트 연결 시 tenant_id 검증 필수 (위 ⚠️ 이월 주의사항 참조)**

**커밋**: `b7bb84a7bdd97b0e6ee15c8dbf26c2f191085227` — "feat: CSV 업로드를 DuckDB 테이블로 적재하는 load_csv_to_table 추가"

---

## Task 4: submissions/consultant_comments 테이블 스키마 — ✅ 완료 (2026-06-08)

**구현 내용**
- `app/database.py`의 `init_schema()` 끝(usage_log 블록 다음, 실제 라인 110~137 — 계획서 명시 라인(103-110)과 약간 차이 있었으나 논리적으로 올바른 위치에 삽입)에 `submissions`(15컬럼)·`consultant_comments`(4컬럼) 테이블 생성 블록 추가
- `tests/test_submission.py`에 `test_submissions_tables_exist_after_init_schema` 추가 (PRAGMA table_info로 컬럼셋 검증)

**테스트 실행 및 결과 (실측)**
```
tests/test_submission.py::test_infer_schema_detects_basic_types PASSED
tests/test_submission.py::test_load_csv_to_table_creates_table_and_returns_preview PASSED
tests/test_submission.py::test_submissions_tables_exist_after_init_schema PASSED
============================== 3 passed in 0.75s ==============================
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — SQL 블록(컬럼명/순서/타입/주석/섹션 코멘트) 및 테스트가 계획서와 character-for-character 일치, 커밋 정확히 2개 파일(+28/+15), 전체 3/3 PASS

**코드 품질 리뷰**: ✅ APPROVED — `rows BIGINT`는 기존 `catalog.rows`와 동일 패턴 재사용(DuckDB 예약어 아님, 충돌 없음 실측 확인), `status VARCHAR -- comment` 표기는 `tenants.gov_type`/`catalog.layer`/`usage_log.action`과 동일한 기존 컨벤션, PRAGMA table_info 기반 테스트는 실제 스키마를 검증(공허한 통과 아님), FK/인덱스 미사용도 기존 테이블들과 일관. 이슈 없음

**커밋**: `891d4ac231e296fead6a1d5c8d3d2652f2e8c6ae` — "feat: submissions/consultant_comments 테이블 스키마 추가"

---

## Task 5: 제출 상태 관리 함수 — app/submission.py (3단계) — ✅ 완료 (2026-06-08)

**구현 내용**
- `app/submission.py`에 `_now`, `create_submission`, `record_decision`, `add_comment`, `get_submission` 5개 함수 추가 (기존 import·PREVIEW_LIMIT 재사용)
- `tests/test_submission.py`에 `_sample_meta` 헬퍼 + 테스트 3개 추가 (생성→조회, 결정→조회, 코멘트→상세조회 라운드트립)
- TDD 순서 준수

**테스트 실행 및 결과 (실측)**
```
[구현 전 — 실패 확인]
E   ImportError: cannot import name 'create_submission' from 'app.submission'
============================== 1 error in 0.77s ===============================

[구현 후 — 통과 확인, 6개 전체]
test_infer_schema_detects_basic_types PASSED
test_load_csv_to_table_creates_table_and_returns_preview PASSED
test_submissions_tables_exist_after_init_schema PASSED
test_create_submission_inserts_row_with_submitted_status PASSED
test_record_decision_updates_status_and_decided_at PASSED
test_add_comment_and_get_submission_includes_comments PASSED
============================== 6 passed in 1.00s ===============================
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — 5개 함수·3개 테스트 모두 byte-for-byte 일치 확인(SQL `?` 플레이스홀더 수가 실제 컬럼 수(submissions=15, consultant_comments=4)와 정확히 일치하는 것까지 검증), 커밋 정확히 2개 파일(+121), 6/6 PASS, 작업트리 클린

**코드 품질 리뷰**: ✅ APPROVED — 파라미터화 쿼리 일관 사용(주입 방지), get_submission의 table_name 인터폴레이션은 Task 3과 동일한 신뢰 경계(controlled source) 패턴, _now()는 기존 코드베이스 타임스탬프 포맷과 동일(오히려 헬퍼화로 DRY 개선), 테스트가 실제 DB 라운드트립 검증. 이슈 없음(import 위치 cosmetic nit만 존재)

**커밋**: `1f7a17b3ae765b70b369e577d7048ac2fa93349e` — "feat: 제출 생성·승인/반려 결정·코멘트·상세조회 함수 추가"

---

## Task 6: 범용 품질 규칙 — generic_rules/run_quality_generic — ✅ 완료 (2026-06-08)

**구현 내용**
- `app/quality.py`에 `RULES_GENERIC_THRESHOLD=5.0`(완화 기준 5%, 데모 3종 전용 `ERROR_RATE_THRESHOLD=0.001`과 별개), `NUMERIC_TYPES` 셋, `generic_rules(table_name)`, `run_quality_generic(table_name)` 추가 — 기존 `RULES`/`run_quality`/`_table_of`/`run_all`은 완전 미변경 (diff: 65 insertions, 0 deletions)
- `tests/test_quality_generic.py` 신규: `_make_test_table()` 헬퍼(결측치·중복행·음수이상치 데이터 포함) + 테스트 2개
- TDD 순서 준수

**테스트 실행 및 결과 (실측)**
```
[구현 전 — 실패 확인]
E   ImportError: cannot import name 'generic_rules' from 'app.quality'
============================== 1 error in ... ===============================

[구현 후 — 신규 2개 통과 확인]
test_quality_generic.py::test_generic_rules_returns_rule_sql_threshold_dicts PASSED
test_quality_generic.py::test_run_quality_generic_reports_detected_issues PASSED
============================== 2 passed in 0.58s ==============================

[전체 스위트 — 회귀 없음 확인]
============================== 8 passed in 1.69s ==============================
(신규 2개 + 기존 test_submission.py 6개, 모두 PASS)
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — `app/quality.py`(lines 36-98, RULES 직후·기존 함수 이전 정확한 위치) 및 `tests/test_quality_generic.py` 모두 계획서와 character-for-character 일치(상수/함수 시그니처/SQL 문자열·따옴표 스타일 `"{name}"`/docstring/테스트 데이터 4행 및 주석 포함). 커밋 `git show --stat` 확인 결과 정확히 2개 파일(+65/+46, 합계 111 insertions·0 deletions), 기존 `RULES`·`ERROR_RATE_THRESHOLD`·`_table_of`·`run_quality`·`run_all` 완전 미변경. 재실행한 `pytest tests/ -v` 8/8 PASS, `git status --porcelain` 클린

**코드 품질 리뷰**: ✅ APPROVED — `run_quality_generic` 반환 dict가 `run_quality`와 동일 키 구조로 호환성 확보(향후 `summarize_quality`에서 상호 교환 가능), `checked = total_rows * len(rules)` 계산이 기존 `run_quality`와 동일한 정의 유지, 컬럼 식별자를 큰따옴표로 감싸는 처리는 한글·공백 포함 동적 컬럼명 대응을 위해 기존 코드보다 오히려 더 안전한 신규 패턴(기존 관행과 충돌 없음), 테스트 데이터가 결측치·중복행·음수이상치 3종 규칙을 모두 의미있게 커버. 마이너 제안만 존재(논블로킹): ① `RULES_GENERIC_THRESHOLD` 명칭이 `ERROR_RATE_THRESHOLD`와의 대응 관계를 가리는 면이 있어 `GENERIC_ERROR_RATE_THRESHOLD` 등으로 개명 고려, ② `generic_rules`가 컬럼명은 큰따옴표 처리하면서 `table_name`은 미처리(기존 관행과 동일하나 신규 함수 내 자체 비일관) — 후속 정리 과제로 기록, 즉시 수정 불요

**커밋**: `585b9a79885cdb8e14e7a7487ea25e32bf585252` — "feat: 임의 업로드 테이블용 범용 품질 진단 규칙 RULES_GENERIC 추가"

---

## Task 7: 품질 요약 문자열 생성 헬퍼 — summarize_quality — ✅ 완료 (2026-06-08)

**구현 내용**
- `app/submission.py` 끝(get_submission 다음)에 `summarize_quality(result: dict) -> str` 추가 — `run_quality_generic()` 결과를 "규칙 N종 / 오류 N건 / 오류율 N% / 통과·미통과" 형태의 한글 요약 문자열로 변환 (제출 레코드의 quality_summary 컬럼에 저장)
- `tests/test_submission.py` 끝에 `test_summarize_quality_formats_pass_and_fail` 추가 (통과/미통과 양쪽 분기 + 포맷 문자열 검증)
- TDD 순서 준수

**테스트 실행 및 결과 (실측)**
```
[구현 전 — 실패 확인]
E   ImportError: cannot import name 'summarize_quality' from 'app.submission'
============================== 1 error in 0.63s ===============================

[구현 후 — 신규 1개 통과 확인]
test_submission.py::test_summarize_quality_formats_pass_and_fail PASSED [100%]
============================== 1 passed in 0.45s ==============================

[전체 스위트 — 회귀 없음 확인, 9개]
test_quality_generic.py::test_generic_rules_returns_rule_sql_threshold_dicts PASSED
test_quality_generic.py::test_run_quality_generic_reports_detected_issues PASSED
test_submission.py::test_infer_schema_detects_basic_types PASSED
test_submission.py::test_load_csv_to_table_creates_table_and_returns_preview PASSED
test_submission.py::test_submissions_tables_exist_after_init_schema PASSED
test_submission.py::test_create_submission_inserts_row_with_submitted_status PASSED
test_submission.py::test_record_decision_updates_status_and_decided_at PASSED
test_submission.py::test_add_comment_and_get_submission_includes_comments PASSED
test_submission.py::test_summarize_quality_formats_pass_and_fail PASSED
============================== 9 passed in 1.38s ==============================
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — `app/submission.py`(get_submission 직후, 파일 끝) 및 `tests/test_submission.py`(import 라인을 최상단 블록과 분리해 새 테스트 직전에 배치) 모두 계획서와 character-for-character 일치(시그니처·docstring·verdict 로직·f-string 줄바꿈/연결·한글 문자열까지). 커밋 `git show --stat` 정확히 2개 파일(+9/+16, 25 insertions·0 deletions), 메시지 일치, 9/9 PASS, `git status --porcelain` 클린

**코드 품질 리뷰**: ✅ APPROVED (재검토 1회 거침) — 1차 리뷰에서 "Important" 2건 제기됨: ① `f"{error_rate}%"`가 과학적 표기(`5e-05%`) 위험 ② run_quality/run_quality_generic 결과 dict의 4-키 서브셋 의존이 타입으로 명시 안 됨. 컨트롤러가 실측 검증(①: 스펙이 명시한 `run_quality_generic()`은 `round(rate,4)`를 사용하며, 4자리 반올림은 수학적으로 과학적 표기를 만들 수 없음을 2백만 샘플 무작위 검색으로 재확인 — 5자리 반올림(`run_quality` 전용, 본 함수의 명시된 계약 밖)에서만 발생; ②: 코드가 계획서와 byte-for-byte 일치하고, submission.py/quality.py 어디에도 TypedDict 등 강한 타입 사용 선례 없어 기존 컨벤션과 일치)을 제시하여 재검토 요청 → 리뷰어가 두 항목 모두 Minor(향후 참고용)로 하향 조정하고 최종 ✅ APPROVED로 변경. 즉시 조치 불필요 — 향후 `run_quality()` 결과와 교차 재사용 시 포맷 명시(`:.4f`) 고려 등 비차단 메모만 존재

**커밋**: `e08ed60ecc4be09bb2b3f585940d17788e508c9c` — "feat: 품질진단 결과 요약 문자열 생성 함수 summarize_quality 추가"

---

## Task 8: 계획서 초안 생성 — app/planning.py — ✅ 완료 (2026-06-08)

**구현 내용**
- `app/planning.py` 신규 생성: 모듈 docstring(LLM 미사용·공공망 대응 명시), `_tenant_name`, `draft_open_plan(tenant_id)`(제출 현황 개방/보류/검토대기 건수 집계 → 개방계획 초안), `_RATE_RE`/`_RULE_RE`/`_ERR_RE` 정규식, `draft_quality_plan(tenant_id)`(반려 건의 quality_summary를 정규식으로 파싱해 개선과제 목록 생성 → 품질개선계획 초안)
- `tests/test_planning.py` 신규: `_seed_submissions` 헬퍼(승인/제출/반려 3건 시드, create_submission+record_decision 실제 API로 구성) + 테스트 2개
- TDD 순서 준수

**테스트 실행 및 결과 (실측)**
```
[구현 전 — 실패 확인]
E   ModuleNotFoundError: No module named 'app.planning'
============================== 1 error in 0.89s ===============================

[구현 후 — 신규 2개 통과 확인]
test_planning.py::test_draft_open_plan_counts_status_by_tenant PASSED [ 50%]
test_planning.py::test_draft_quality_plan_lists_failed_submissions PASSED [100%]
============================== 2 passed in 1.24s ==============================

[전체 스위트 — 회귀 없음 확인, 11개]
test_planning.py::test_draft_open_plan_counts_status_by_tenant PASSED
test_planning.py::test_draft_quality_plan_lists_failed_submissions PASSED
test_quality_generic.py::test_generic_rules_returns_rule_sql_threshold_dicts PASSED
test_quality_generic.py::test_run_quality_generic_reports_detected_issues PASSED
test_submission.py:: (6개 모두) PASSED
============================= 11 passed in 2.44s ==============================
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — `app/planning.py`·`tests/test_planning.py` 둘 다 계획서와 character-for-character 일치(docstring·정규식 3종(미사용 `_RULE_RE` 포함 보존)·SQL 쿼리·f-string·한글 텍스트·`or [...]` 빈 목록 처리 관용구까지). 커밋 `git show --stat` 정확히 신규 파일 2개(+87/+47, 134 insertions·0 deletions), 메시지 일치, 11/11 PASS, `git status --porcelain` 클린

**코드 품질 리뷰**: ✅ APPROVED (비차단 후속과제 권고) — 파라미터화 쿼리·`lines→"\n".join` 빌드 패턴·docstring 스타일 모두 기존 코드베이스(submission.py/quality.py/evaluation.py) 컨벤션과 일치, 시드 데이터가 실제 API(create_submission/record_decision)로 구성된 양질의 통합 테스트. 비차단 메모 3건: ① `_RULE_RE` 미사용 — 스펙 명시대로 보존(향후 정리 과제로 기록), ② **`draft_quality_plan`이 `summarize_quality()` 생성 문자열을 정규식으로 역파싱하는 라운드트립 설계 — Task 9 담당자 참고용으로 ⚠️ 이월 주의사항에 추가 기록**, ③ `_tenant_name` 폴백(미등록 tenant_id 그대로 표시)·빈 상태 처리 관용구 3종 혼재는 현재 범위에선 합리적

**커밋**: `b1f3066d19360f5c7d9ef6e394dd27fe7e783199` — "feat: 개방계획·품질개선계획 초안 자동 생성 모듈 추가"

---

## Task 9: API 8종 — app/main.py 라우트 추가 — ✅ 완료 (2026-06-08)

**구현 내용**
- `app/main.py`에 `# ---------- 공급자 워크플로우 ----------` 섹션으로 8개 라우트 추가: `submission_upload`(POST `/api/submission/upload`), `submission_create`(POST `/api/submission`), `submission_list`(GET `/api/submission`), `submission_list_all`(GET `/api/submission/all`), `submission_decision`(POST `/api/submission/{id}/decision`), `submission_comment`(POST `/api/submission/{id}/comment`), `submission_detail`(GET `/api/submission/{id}`), `plan_draft`(GET `/api/plan/draft`)
- import 추가: `FastAPI, Query, UploadFile, File, Form` / `seed_data, quality, evaluation, ontology, nlquery, submission, planning`
- `requirements.txt`에 `python-multipart>=0.0.9` 추가 (FastAPI Form/File 처리에 필요)
- `submission_upload`에 ⚠️ 이월주의 ① 가드 최초 적용: `tenant_id`를 `tenants` 테이블에서 검증 후 `new_table_name` 호출 (Task 3 코드품질 리뷰 권고사항 반영)

**테스트/검증 (실측)** — pytest 단위테스트 대상 모듈 없음(API 라우트는 계획서 명시대로 서버 실행+수동 스모크테스트로 검증)
```
[전체 회귀 스위트 — 11/11 PASS, API 추가로 인한 회귀 없음 확인]
============================== 11 passed in ... ===============================

[서버 기동 + Swagger(openapi.json) 확인]
GET /openapi.json → 8개 신규 경로 모두 등록 확인
  /api/submission/upload, /api/submission, /api/submission/all,
  /api/submission/{submission_id}/decision, /api/submission/{submission_id}/comment,
  /api/submission/{submission_id}, /api/plan/draft

[수동 스모크테스트 — 8개 라우트 각각 정상 응답 확인 (요청/응답 verbatim 기록은 구현 서브에이전트 보고서 참조)]
- 업로드→등록→목록→상세→결정→코멘트→계획서초안 전체 플로우 200 정상
- 알 수 없는 tenant_id → 400 정상 반환
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — 8개 라우트 시그니처·응답 구조·HTTP 메서드·경로가 계획서와 일치, import/requirements.txt 변경사항도 계획서 명시 범위 내. 커밋이 계획서가 지정한 파일만 포함

**코드 품질 리뷰**: ❌ CHANGES REQUESTED → (보안수정 후 재검토) → ✅ APPROVED
- **1차 리뷰에서 Important 1건 발견**: `submission_create`(POST `/api/submission`)가 `table_name: str = Form(...)`을 클라이언트로부터 검증 없이 직접 받아 `quality.run_quality_generic(table_name)`·`submission.create_submission(..., table_name=table_name)`에 그대로 전달 → 두 함수 모두 `table_name`을 SQL 문자열에 f-string 인터폴레이션(`PRAGMA table_info('{table_name}')`, `SELECT count(*) FROM {table_name} ...`, `SELECT * FROM {table_name} LIMIT ...`)하므로, **`submission_upload`에서 막았던 것과 동일한(오히려 영구 저장되어 `submission_detail` 조회 때마다 재실행되므로 더 심각한) 임의 테이블 접근/SQL 인젝션 벡터가 재발**한다는 지적
- **수정 내용**: `submission_create` 최상단(`meta` 구성 이전)에 가드 2개 추가
  1. `tenant_id`를 `tenants` 테이블에서 검증(미존재 시 400 `{"error": "알 수 없는 tenant_id"}`) — `submission_upload`와 동일 패턴
  2. 모듈 레벨 정규식 `_TABLE_NAME_RE = re.compile(r"^sub_(.+)_[0-9a-f]{8}$")`로 `table_name`이 `submission.new_table_name(tenant_id)`이 생성하는 `sub_{tenant_id}_{8자리hex}` 형태와 정확히 일치하는지, 그리고 그 안의 tenant 구간이 검증된 `tenant_id`와 문자열 동일한지(`m.group(1) != tenant_id`) 검사 — 불일치 시 400 `{"error": "유효하지 않은 table_name"}`
  - 새 커밋 `cbfc907` ("fix: submission_create의 table_name 검증 추가 (임의 테이블 접근 차단)") — `app/main.py`만 +14줄 변경
- **수정 검증(실측)**: pytest 11/11 재확인(수정 전후 모두 green) + 스모크테스트 4종
  - `table_name=tenants`(내부 테이블 프로빙 시도) → 400 `{"error": "유효하지 않은 table_name"}`, 레코드 미저장 확인
  - `table_name=sub_48121_deadbeef`(타 tenant 패턴 도용 시도, tenant_id=48310으로 요청) → 400 동일 거부, 미저장 확인
  - 알 수 없는 `tenant_id=99999` → 400 `{"error": "알 수 없는 tenant_id"}`
  - 정상 플로우(업로드→`sub_48310_579fccad` 발급→해당 tenant_id+table_name으로 등록) → 200 정상 저장 확인
- **재검토 결과**: ✅ APPROVED — 가드가 SQL 터치 코드(`run_quality_generic`/`create_submission`) 이전에 위치, 정규식 양끝 앵커링(`^...$`)으로 prefix/suffix 스머글링 차단, `m.group(1) != tenant_id`는 평문자열 비교(정규식 아님)라 메타문자 인젝션 우려 없음, `tenant_id`는 `tenants` 화이트리스트로 사전 검증되어 항상 숫자 SGG 코드 형태(언더스코어·특수문자 불포함)임을 그렇지 않더라도 정규식+문자열비교 조합 자체가 안전함을 확인. `submission_detail`이 영구저장된 `table_name`을 재사용하는 것도 — 이제 `create_submission`(유일한 writer)이 `sub_<tenant>_<hex>` 형태만 저장 가능하므로 — 안전하다는 추론이 타당함을 재확인. 회귀 없음(11/11 유지)

**커밋**: `06379ac` — "feat: 공급자 워크플로우 API 8종 라우트 추가" / `cbfc907` — "fix: submission_create의 table_name 검증 추가 (임의 테이블 접근 차단)"

---

## Task 10: 화면 — 새 탭 "데이터 등록·관리" 골격 추가 — ✅ 완료 (2026-06-08)

**구현 내용**
- `web/index.html`에 탭 버튼(`<button data-tab="submission">데이터 등록·관리</button>`)과 섹션 골격(`<section id="tab-submission" class="hidden">` — 제목/부제/모드토글(`#sub-mode-toggle`, `data-mode="agency"|"center"`)/뷰 컨테이너(`#sub-agency-view`, `#sub-center-view`)) 추가 — 순수 HTML, JS/CSS/백엔드 변경 없음 (+14줄)

**검증 (실측, Playwright MCP 브라우저 자동화)**
```
[서버 기동 — 워크트리 디렉터리에서 정확히 실행한 것 확인 (포트 충돌로 떠있던 타 디렉터리발 stale 서버 PID 13468 종료 후 재기동, PID 14916)]
Uvicorn running on http://127.0.0.1:5959

[브라우저 내비게이션 + 탭 클릭 — 스냅샷 확인]
- "데이터 등록·관리" 버튼이 nav 6번째(마지막) 탭으로 등장, 클릭 시 [active] 전환
- 섹션 렌더링 확인: 제목 "데이터 등록·관리", 부제 전문, 칩 버튼 2개
  ("기관 담당자 모드" / "센터(컨설팅) 모드") 모두 정상 표시
- "센터(컨설팅) 모드" 클릭 — 동작 없음(Task 11/12 로직 이전이므로 정상/예상된 상태)

[콘솔 메시지 점검]
페이지/JS 관련 에러 0건 (favicon.ico 404는 기존부터 존재하던 무관한 항목)

[스크린샷] 다크블루 헤더 nav에 새 탭 활성화(흰색 강조), 제목·부제·필 형태 토글버튼 2개 정상 렌더링 확인
서버 정상 종료(PID 14916 kill, 포트 5959 listening 해제 확인)
```

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — 버튼이 `자연어 질의` 버튼 직후·`</nav>` 직전, 섹션이 `tab-ai`의 `</section>` 직후·`</main>` 직전에 정확히 삽입됨. 모든 한글 문자열이 계획서와 character-for-character 일치(한자 0건, 프로그래밍 방식 CJK 통합 한자 범위 스캔으로 확인). `id`/`class`/`data-*` 속성 전부 정확. `git show --stat` 정확히 `web/index.html` 1개 파일 +14줄만 변경, 커밋 메시지 일치(한자 없음)

**코드 품질 리뷰**: ✅ APPROVED — 기존 탭들(`tab-ai`, `tab-ontology` 등)과 구조·클래스 네이밍(`page-title`/`page-sub`/`chips`/`hidden`/`active`) 완전 일치, ID 충돌 없음(전체 파일 스캔 확인), `id="tab-submission"`은 기존 범용 탭전환 로직(`app.js` 8-13행 `$('#tab-' + b.dataset.tab)...`)과 무수정으로 바로 연동됨, `data-mode="agency"/"center"`·`sub-` 접두사 ID들은 Task 11/12의 JS가 자연스럽게 바인딩할 수 있는 관용적 훅. 마이너 1건(논블로킹): 다른 섹션엔 있는 `<!-- SUBMISSION -->` 같은 HTML 주석 헤더가 누락 — 후속 정리 과제로 기록 가능, 차단 사유 아님

**커밋**: `163b862aada36bfd0bb2c9f7387866298b561f54` — "feat: '데이터 등록·관리' 탭 골격 추가"

---

## Task 11: 화면 — 모드 토글 + 기관 담당자: 새 데이터 등록 폼 — ✅ 완료 (2026-06-08)

**구현 내용**
- `web/app.js` 끝에 `// ---------- 데이터 등록·관리 탭 ----------` IIFE 모듈 추가(184줄): `initSubmissionTab`(모드토글 바인딩)·`renderAgencyView`(업로드/메타등록/내제출현황/계획서초안 카드 렌더)·`loadMySubmissions`·`openSubmissionDetail`(상세 모달+승인/반려)·`decide`·`renderCenterView`(자리표시자) + `window.initSubmissionTab` 전역 노출
- 기존 탭전환 핸들러(`/* ---------- 탭 전환 ---------- */`)에 `if (b.dataset.tab === 'submission') window.initSubmissionTab();` 1줄 연결
- `TENANT_ID="48121"`(창원시 데모 고정, 로그인 미구현 단계 — 기존 `loadOntology`의 `drawGraph('48121')`과 동일 데모 테넌트)

**테스트/검증 (실측, Playwright MCP 브라우저 자동화 — 전체 플로우 1차 통과)**
```
[흐름 1~7 전부 정상 동작 확인]
1. "데이터 등록·관리" 탭 클릭 → 모드토글·업로드폼·내제출현황·계획서초안 카드 렌더 확인
2. CSV(지역명,인구수 / 창원시,100 / 진주시,80) 업로드
3. "업로드 & 미리보기" → "스키마 추론 완료: 2행, 컬럼 2개..." 노트 + 메타폼 표시 확인
4. 메타정보 입력(순한글) 후 "등록(자동 진단 실행)" → "등록 완료 — 자동 진단 결과: 규칙 4종 / 오류 0건 / 오류율 0.0% / 통과" 확인
5. "내 제출 현황"에 submitted 배지로 신규 항목 표시 확인
6. 항목 클릭 → 모달(승인/반려 버튼·결정메모·센터코멘트 섹션) → "승인(개방)" 클릭 → 모달 닫힘 + 배지 approved로 전환 확인
7. "공공데이터 개방계획 초안"/"품질개선계획 초안" 버튼 → #plan-draft-output에 초안 텍스트 정상 출력 확인
콘솔 에러 0건(기존 무관 favicon 404 제외)
```
(※ 구현 중 1회 — Playwright 세션의 브라우저 디스크캐시로 구버전 app.js가 로드되는 환경 이슈 발견·진단(`performance.getEntriesByType('resource')`로 transferSize=0/구버전 바이트수 확인) → 페이지 재오픈+캐시버스팅으로 해결, 신규 코드 자체는 무관함을 별도 fetch+eval로 검증)

**스펙 일치 리뷰**: ✅ SPEC COMPLIANT — 추가된 184줄 모듈을 계획서 코드(plan.md L1083-1265)와 프로그래매틱 diff 수행 결과 **완전 동일**(0 차이), 탭전환 1줄 와이어링도 byte-for-byte 일치, 한자 0건(전체 추가라인 CJK 통합한자 범위 스캔), 커밋이 `web/app.js` 1개 파일 +185줄(와이어링 1+모듈 184)만 포함, 메시지 정확

**코드 품질 리뷰**: ❌ CHANGES REQUESTED → (보안수정 후 재검토) → ✅ APPROVED
- **1차 리뷰에서 Important 1건 발견— 저장형 XSS**: 기존 `app.js`엔 서버 데이터를 `innerHTML`에 넣기 전 이스케이프하는 `esc()` 헬퍼가 28회 사용되는 확립된 관행이 있으나, 이 신규 모듈은 `r.title`/`r.quality_summary`/`m.title`/`m.description`/`m.quality_summary`/`c.comment` 6개 필드를 이스케이프 없이 `innerHTML`에 인터폴레이션. **결정적으로, 이 모듈은 코드베이스에서 유일하게 "실제 사용자가 입력한 문자열"이 백엔드를 거쳐 `innerHTML`로 되돌아오는 경로**(기관담당자가 `#meta-form`에 입력한 title/description, `decision_note`/`comment`가 `Form(...)`으로 무검증 저장 후 재출력)이며, 특히 `openSubmissionDetail` 모달은 **센터(컨설턴트) 직원도 열람**하므로 권한경계를 넘는 저장형 XSS(예: 제목에 `<img src=x onerror=...>` 입력 시 이를 열람하는 센터 직원 브라우저에서 실행)가 성립한다는 지적
- **수정 내용**: 새 커밋 `bbd9c21`("fix: 기관 담당자 모드 UI의 사용자 입력값 HTML 이스케이프 적용 (저장형 XSS 방지)") — 기존 `esc()` 헬퍼(신규 정의 없음)로 7개 인터폴레이션 지점 래핑: `loadMySubmissions`의 `esc(r.title)`/`esc(r.quality_summary || '')`, `openSubmissionDetail`의 `esc(m.title)`/`esc(m.description)`/`esc(m.quality_summary || '')`/`esc(c.comment)` — `app.js` 1개 파일 +6/-6줄만 변경 (`c.created_at`은 서버타임스탬프, `data.draft`는 이미 `.textContent` 사용이라 그대로 보존)
- **수정 검증(실측, 페이로드 레벨)**:
  - title=`<b>테스트</b>제목`, description=`설명&특수문자<태그>`로 등록 → 목록·모달 양쪽에서 `innerHTML="&lt;b&gt;테스트&lt;/b&gt;제목"` 등 escaped 형태로 렌더링(`hasNestedB:false`) — 리터럴 텍스트로 표시됨을 직접 확인(스크린샷)
  - `fetch()`로 `<img src=x onerror=alert(1)>코멘트테스트&특수<문자>` 코멘트 직접 주입 → 모달 재오픈 시 `innerHTML="- &lt;img src=x onerror=alert(1)&gt;..."`, `hasImg:false`, onerror 미실행 — 페이로드가 무력화됨을 확인
  - 정상플로우(업로드→등록→진단→목록→승인→계획서초안) 재검증, 회귀 없음, 콘솔 에러 0건
- **재검토 결과**: ✅ APPROVED — 7개 지점 모두 `esc(...)`로 정확히 래핑(오타·괄호불일치 없음), `esc`는 신규정의 아닌 기존 헬퍼(28회→30회 사용), 모두 텍스트노드 컨텍스트(속성 컨텍스트 아님 — `&<>` 이스케이프로 충분)에 위치, `r.submission_id`(서버생성 UUID)·`r.status`(고정 enum)는 사용자 자유입력이 아니므로 이스케이프 불필요가 타당함을 확인, 정상 제목(`2026년 인구 현황` 등 `&<>` 미포함)은 출력 변화 없음(회귀 없음). 비차단 메모만 존재(2차 리뷰에서 추가 지적하지 않음 — 1차 리뷰의 Minor 4건(fetch 에러처리·모달 바깥클릭닫기 등)은 본 수정 범위 밖으로 재론하지 않음)

**커밋**: `7fc86b03ad5fa743303eeefede88a439fa833073`(UI 모듈) / `bbd9c2145e8b521460b58e3bb89e30e53d4a48f5`(XSS 이스케이프 수정)

---

## 재개 방법 (다른 PC/AI용)

1. 저장소 클론 또는 pull: `git clone https://github.com/baramgay/eum` (또는 기존 클론에서 `git fetch && git checkout feature/공급자-워크플로우 && git pull`)
2. 의존성 설치: `pip install -r requirements.txt`
3. 위 표에서 "⏳ 대기" 중 가장 위 태스크부터 계획서(`2026-06-08-공급자워크플로우-plan.md`)의 해당 섹션 전문을 읽고 구현
4. 완료 후 본 로그 파일에 표 상태 갱신 + 해당 태스크 섹션(구현내용/테스트 실측 출력/리뷰 결과/커밋 SHA) 추가
5. `pytest tests/ -v` 전체 실행 결과를 각 태스크 기록에 실측값으로 남길 것 (요약·생략 금지 — 실제 출력 그대로)
